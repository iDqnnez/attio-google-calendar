## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles use matching label names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Inngest

When adding or changing Inngest code, follow the skills under `.agents/skills/` (`inngest-setup`, `inngest-events`, `inngest-steps`, `inngest-flow-control`, `inngest-durable-functions`).
