# Attio is Authority; Calendar is a Projection

This project applies Attio Task state onto Google Calendar Events. It does not write Calendar changes back to Attio. True two-way merge is deferred so the first version stays a reliable one-way Projection with a durable Binding that reverse Projection can hook into later.
